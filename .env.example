# GitHub Organizations to search for repositories
# Comma-separated list of GitHub organization names
GITHUB_ORGS="mbernier,bernierllc,unicorn"

# Projects directory
# Base directory where projects are stored
PROJECTS_DIR="$HOME/projects"

# Custom directories (optional)
# Comma-separated list of alternative directories to check for projects
CUSTOM_DIRS="/unicorn"
